{
    "name": "Beyond Blocks",
    "image": "mcr.microsoft.com/devcontainers/python:3.12",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y espeak-ng alsa-utils && pip install -e ."
}